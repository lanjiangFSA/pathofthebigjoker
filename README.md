# 大怪路子 Web 版

> 初始版本：v0.1.0

一个无需安装的浏览器联机大怪路子牌桌。房主创建房间并分享六码房间号；人数不足六人时，空位会由 AI 自动补齐。

## 快速启动

需要 Node.js 18 或更新版本。在项目根目录执行：

```bash
node server.js
```

浏览器访问 `http://localhost:3000`。停止服务请在终端按 `Ctrl + C`。

## v0.1.0 功能

- Web 浏览器多人房间：创建、加入、邀请码复制与服务端实时同步。
- 六人、三副牌、红蓝交叉座位；未坐满时由 AI 补位（主攻/辅助分工）。
- 单张、对子、三张，以及杂顺、同花、三带两、四带一、同花顺、五同。
- 牌序：大怪 > 小怪 > 将牌 > A > K … > 2；手牌左大右小。
- 自由牌可组非单张牌型；五路中按最小补齐，并优先判成更大牌型。
- 回合出牌、压牌、过牌与重新获得出牌权。
- 创建房间可选「启用将牌升级」（默认关闭，将牌固定为 2）。
- 红蓝比分从 0 累计；座位剩余张数仅在 ≤10 张时显示。
- 手牌重叠扇形（优先露出点数花色，过窄才换行）；可拖动多选。
- 桌面固定显示「谁出了什么」。

## 当前范围

规则对齐 `Skills/howtoplay.md`；AI 配合对齐 `Skills/howtocollabrate.md`。问牌/报牌等进阶口头约定尚未做成独立交互。本地可用 `npm test` 跑规则与配合用例。

## 免费部署

项目包含 `render.yaml`，可作为 Render Blueprint 部署：

1. 将此 Git 仓库推送到 GitHub。
2. 登录 Render，选择 **New → Blueprint** 并连接仓库。
3. 创建免费 Web Service；Render 会提供一个可分享的 HTTPS 地址。

注意：Render 免费实例在连续 15 分钟无访问后会休眠，首次重新访问约需一分钟唤醒。

## 开发与版本

```powershell
git log --oneline
git status
```

提交信息遵循 Conventional Commits 风格，例如 `feat(game): ...`、`fix(game): ...`、`docs: ...`。
